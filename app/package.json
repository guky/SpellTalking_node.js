{
     "name": "app",
     "description": "example chat application with socket.io",
     "version": "0.0.1",
	 "scripts": {
    "start" : "node app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
	},
     "dependencies": {
        "express": "2.4.6",
        "socket.io": "0.8.4"
     }
}